ДЗ 2_1_2_4
---------- это директории
**/.terraform/* -- тут мы игнорируем все файлы, который будут встречаться от каталога Terraform, где будет вложенный скрытый каталог .terraform
*.tfstate -- игнорируем все файлы в каталоге Terraform с таким расширением
*.tfstate.* -- игнорируем все файлы в названии который будет присутствовать эта маска
----------- это логи
crash.log -- игнорируем файлы с этим названием, находящиеся в этом каталоге
*.tfvars -- игнорируем все файлы с этим типом файла, находящиеся в этом каталоге
----------- рабочие файлы (транлоги наверно)
override.tf -- игнорируем такие файлы в этом (Terraform/) каталоге
override.tf.json -- аналогично
*_override.tf -- игнорируем все файлы с этим расширеним
*_override.tf.jso --аналогично 
----------- это конфиги
.terraformrc -- игнорируем файлы с этим расширением
terraform.rc -- игнорируем эти файлы
